# Archivo de utilidades

Esta carpeta conserva scripts auxiliares que se usaron durante el desarrollo, pero que no forman parte del flujo principal del TFM.

- `add_notebook_code_explanations.py`: generaba explicaciones automaticas dentro de notebooks. Se archiva porque el entregable final mantiene notebooks limpios, sin celdas generadas automaticamente.
